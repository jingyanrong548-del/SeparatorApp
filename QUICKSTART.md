# 快速启动指南

## 🚀 开发环境

```bash
# 1. 安装依赖（首次运行）
npm install

# 2. 启动开发服务器
npm run dev
```

开发服务器会自动在 http://localhost:3000 打开，支持：
- ✅ 热模块替换 (HMR)
- ✅ 快速刷新
- ✅ 现代化开发体验

## 📦 构建生产版本

```bash
# 构建
npm run build

# 预览构建结果
npm run preview
```

构建产物在 `dist/` 目录。

## 🛠️ Vite 的优势

1. **极速启动**：毫秒级启动开发服务器
2. **热更新**：修改代码立即看到效果
3. **现代化工具链**：支持最新的 ES 特性
4. **优化构建**：生产构建自动优化
5. **插件生态**：丰富的插件支持

## 📁 项目结构

```
SeparatorApp/
├── index.html          # 入口 HTML
├── vite.config.js     # Vite 配置
├── package.json       # 项目配置
├── src/               # 源代码
│   ├── script.js     # 主逻辑
│   └── style.css     # 样式
└── public/            # 静态资源
    ├── coolprop.js   # CoolProp 库
    └── coolprop.wasm # WASM 文件
```

## 🔧 常用命令

```bash
npm run dev      # 开发模式
npm run build    # 构建生产版本
npm run preview  # 预览生产构建
```

## 🌐 Vercel 部署

代码推送到 GitHub 后，Vercel 会自动：
1. 检测到 Vite 项目
2. 运行 `npm run build`
3. 部署 `dist/` 目录

无需额外配置！

